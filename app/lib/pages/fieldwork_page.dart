import 'package:flutter/material.dart';

class FieldworkPage extends StatelessWidget {
  const FieldworkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("FieldworkPage"),
      ),
    );
  }
}
