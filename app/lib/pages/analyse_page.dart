import 'package:flutter/material.dart';

class AnalysePage extends StatelessWidget {
  const AnalysePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("AnalysePage"),
      ),
    );
  }
}
